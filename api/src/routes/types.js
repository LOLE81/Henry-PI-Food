const { Router } = require('express');


const router = Router();

router.get('/', async (req, res, next) => {
    const dietType = 
    res.send('--------- get / diet types')  
})



module.exports = router;